import { CardNumberInputFormatDirective } from './card-number-input-format.directive';

describe('CardNumberInputFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new CardNumberInputFormatDirective();
    expect(directive).toBeTruthy();
  });
});
